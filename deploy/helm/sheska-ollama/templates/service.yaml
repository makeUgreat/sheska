apiVersion: v1
kind: Service
metadata:
  name: {{ include "sheska-ollama.fullname" . }}
  labels:
    {{- include "sheska-ollama.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sheska-ollama.selectorLabels" . | nindent 4 }}
