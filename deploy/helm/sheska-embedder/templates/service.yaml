apiVersion: v1
kind: Service
metadata:
  name: {{ include "sheska-embedder.fullname" . }}
  labels:
    {{- include "sheska-embedder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sheska-embedder.selectorLabels" . | nindent 4 }}
