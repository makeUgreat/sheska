replicaCount: 2
secret:
  name: sheska-api-secret
image:
  repository: willom2c/sheska-api
  pullPolicy: IfNotPresent
  tag: "sha-0cf8632e357ffc681479cb3ab4e1a85abed87506"
service:
  type: ClusterIP
  port: 3000
ingress:
  enabled: true
  className: ""
  annotations: {}
  hosts:
    - host: api-sheska.meogle.co.kr
      paths:
        - path: /
          pathType: Prefix
  tls: []
resources:
  requests:
    memory: 512Mi
embedding:
  baseUrl: http://sheska-ollama.sheska.svc.cluster.local:11434
nodeSelector: {}
tolerations: []
affinity:
  nodeAffinity:
    requiredDuringSchedulingIgnoredDuringExecution:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: NotIn
              values:
                - hash-n97-1
topologySpread:
  enabled: true
  maxSkew: 1
  topologyKey: kubernetes.io/hostname
  whenUnsatisfiable: DoNotSchedule
  matchLabelKeys:
    - pod-template-hash
