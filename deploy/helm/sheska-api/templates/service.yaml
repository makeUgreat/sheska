apiVersion: v1
kind: Service
metadata:
  name: {{ include "sheska-api.fullname" . }}
  labels:
    {{- include "sheska-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sheska-api.selectorLabels" . | nindent 4 }}
