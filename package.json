{
  "name": "sheska",
  "version": "0.0.1",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "deps:check": "depcruise apps --config dependency-cruiser/config.cjs",
    "harness:static": "pnpm deps:check && pnpm -r --if-present harness:static"
  },
  "devDependencies": {
    "dependency-cruiser": "^17.4.2"
  }
}
