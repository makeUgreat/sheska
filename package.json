{
  "name": "sheska",
  "version": "0.0.1",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "deps:check": "depcruise apps --config .dependency-cruiser.cjs",
    "harness:static": "pnpm deps:check && pnpm -r --if-present harness:static",
    "harness:build": "pnpm -r --if-present harness:build && pnpm --filter @sheska/api test:runtime:start && pnpm --filter @sheska/api test:runtime:stop"
  },
  "devDependencies": {
    "dependency-cruiser": "^17.4.2"
  }
}
